spring:
  application:
    name:QuoraApp
  data:
    mongodb:
      host: localhost
      port: 27017
      database: quora_db
      auto-index-creation: true

elasticsearch:
  rest:
    uris: http://localhost:9200
    connection-timeout: 1000
    read-timeout: 3000